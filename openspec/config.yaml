schema: spec-driven

context: |
  Tech stack: Python 3.11+, Textual (TUI), openai SDK (LLM), pydantic (config), loguru (logging), pytest (testing)
  Project: TUI 终端编码 Agent — 从零实现的 AI 编码助手
  Conventions: Python 类型注解, 异步优先, 自实现核心逻辑 (禁止 Agent SDK/Framework)
  Domain: CLI 工具 / AI Agent / 终端应用

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
